# Задачи по аудиту кода QuizMaster

## ✅ Выполнено (Критические)

- [x] **#2** `QuizScreen.js` — Убрать `totalTime` из зависимостей автосохранения. Теперь прогресс пишется только при смене вопроса или ответа, а не каждую секунду.
- [x] **#3** `QuizScreen.js` — Убрать `questions` из payload автосохранения. Вопросы уже хранятся в `.dat` файле на диске — дублировать их в AsyncStorage избыточно и небезопасно.
- [x] **#6** `QuizScreen.js` + `App.js` — Исправить подсчёт мультиответов. Теперь используется сравнение множеств (`Set`), а не только первого выбранного варианта. Это позволяет корректно оценивать вопросы с несколькими правильными ответами.
- [x] **Побочное исправление** `App.js` — При восстановлении прогресса вопросы теперь перечитываются из `.dat` файла, а не из payload (где их больше нет). Добавлен параметр `navigateToQuiz` в `loadQuizFromDatFile`.
- [x] **#4** `App.js` — Множественные GitHub API запросы при старте. Добавлен флаг `isAppReady`.
- [x] **#1** `QuizScreen.js` — Двойной подсчёт времени. Теперь фиксируется только при первом ответе.
- [x] **#5** `App.js` — `checkForUpdates()` дебаунс (1.5 сек) для изменений подписок.

---

## 📋 Оставшиеся задачи по приоритету

### 🟡 Средний приоритет

### 🟢 Низкий приоритет

- [x] **#7** `QuizScreen.js` (строки 365–371) — Кнопка «Завершить» на последнем вопросе. Теперь показывает «⚠️ Завершить», если текущий вопрос не отвечен.
- [x] **#8** `App.js` — Конфликт имён файлов в папке `TEACHER/`. Теперь тесты каждого учителя хранятся в подпапке (по owner ID).
- [x] **#9** `App.js` — Монолитный файл (~3700 строк). Рекомендуется разбить на отдельные хуки и компоненты. (Выполнено: выделены хуки useConfig, useGitHubSync, useLibraryManager и основные экраны).
- [x] **#10** `App.js` — `useEffect([screen])` для синхронизации. Заменено на обертку `setScreen`, вызывающую синхронизацию явно.
- [x] **#11** `utils.js` (строка 95) — Хрупкий regex. Теперь в библиотеке и при сохранении приоритет отдается заголовку `METADATA=`.
- [x] **#12** `QuizScreen.js` (строка 281) — Прогресс-бар показывает «текущую позицию» вместо «количества отвеченных».
- [x] **#13** `QuizScreen.js` — Добавлена плавная анимация (fade) при переходе между вопросами.
- [x] **#14** `QuizScreen.js` — Кнопка «Назад» скрывается на первом вопросе.
- [x] **#15** `App.js` (экран prestart) — Добавлен индикатор загрузки при восстановлении прогресса или перезапуске теста.
